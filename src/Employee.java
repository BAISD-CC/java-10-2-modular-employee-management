// Create a class for Employee
    // Create String attributes for Employee for name, id, and a double for salary.

    /* The no-arg constructor initializes the object with
       empty strings for name and ID, and 0.0 for salary. */

    /* This constructor initializes the object with a name, ID, and salary.
       @param n The name.
       @param i The ID.
       @param s The salary. */

    /* The setName method sets the name field.
       @param n The name to use. */

    /* The setID method sets the ID field.
       @param i The ID to use. */

    /* The setSalary method sets the salary field.
       @param s The salary to use. */

    /* The getName method returns the name field.
       @return The name. */

    /* The getID method returns the ID field.
       @return The ID. */

    /* The getSalary method returns the salary field.
       @return The salary. */