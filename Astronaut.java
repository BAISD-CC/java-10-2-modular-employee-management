/* The Astronaut class stores data about an astronaut,
   which is a specific type of employee, for the
   Employee and Astronaut Classes programming challenge. */

    // Create String attributes for missionName and missionRole

    /* The no-arg constructor initializes the object with
       empty strings for name, ID, missionName, and missionRole,
       and 0.0 for salary. */

    /* This constructor initializes the object with
       a name, ID, salary, mission name, and mission role.
       @param n The name.
       @param i The ID.
       @param s The salary.
       @param m The mission name.
       @param r The mission role. */

    /* The setMissionName method sets the missionName field.
       @param m The mission name to use. */

    /* The setMissionRole method sets the missionRole field.
       @param r The mission role to use. */

    /* The getMissionName method returns the missionName field.
       @return The mission name. */

    /* The getMissionRole method returns the missionRole field.
       @return The mission role. */